name: waza-platform
metadata:
  template: waza-platform
services:
  platform:
    host: containerapp
    language: docker
    docker:
      path: Dockerfile.platform
      context: .
      remoteBuild: true
