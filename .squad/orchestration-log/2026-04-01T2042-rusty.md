# Orchestration Log: Rusty (2026-04-01T2042)

## Task
Add Re-Run button to RunDetail.tsx UI for triggering re-runs from the frontend

## Routing Rationale
- **Domain:** Frontend UI, web dashboard interaction, user workflows
- **Agent:** Rusty (Lead / Architect)
- **Reason:** Leadership + full-stack perspective — Rusty owns design decisions, component architecture, and frontend patterns

## Outcome
**SUCCESS**

- Implemented blue Re-Run button in RunDetail page header
- Button only appears on completed runs
- Calls `POST /api/runs/rerun/{id}` endpoint
- On success, navigates user to new run detail page
- On failure, displays inline error banner
- Integrated with existing API client and hooks

## Impact
Re-run feature now accessible from UI. Users can re-execute previous evals directly from the run detail view with full run context visible.

## Files Modified
- `web/src/api/client.ts` — new `rerunRun` API call
- `web/src/hooks/useApi.ts` — new hook for rerun mutation
- `web/src/components/RunDetail.tsx` — Re-Run button, error handling

## Design Decision Captured
Re-Run button on detail page only (not RunsTable). Table uses click-to-navigate pattern; adding actions would require actions column and create interaction ambiguity.
