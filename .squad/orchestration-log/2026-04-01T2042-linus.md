# Orchestration Log: Linus (2026-04-01T2042)

## Task
Add `POST /api/runs/rerun/{id}` endpoint for re-running evals on the platform

## Routing Rationale
- **Domain:** REST API, backend execution workflow, platform run management
- **Agent:** Linus (Backend Developer)
- **Reason:** Core competency — Linus owns API routes, run execution pipeline, and platform backend

## Outcome
**SUCCESS**

- Implemented `POST /api/runs/rerun/{id}` handler
- Route follows `{action}/{id}` pattern (consistent with existing `cancel/{id}`)
- Handler clones run config from existing run
- Creates new queued run in Cosmos DB
- Dispatches execution via platform orchestrator

## Impact
Re-run feature now has complete backend infrastructure. Enables users to execute a previous eval again without manual config re-entry.

## Files Modified
- `internal/platform/api/handlers.go` — new `handleRerun` handler
- `internal/platform/api/routes.go` — new route registration

## Decision Captured
Route pattern: `POST /api/runs/{action}/{id}` (not `{id}/{action}`) to avoid Go ServeMux ambiguity conflicts
